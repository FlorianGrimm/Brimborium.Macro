{{ 
    if (args.size) > 0
        text = args[0]
    end
    if (args.size) > 1
        repeats = args[1]
    end
    for index in 1..repeats }}
Console.WriteLine("{{ text }}"); // {{ index }}
{{   end }}
